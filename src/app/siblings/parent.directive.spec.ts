import { ParentDirective } from './parent.directive';

describe('ParentDirective', () => {
  it('should create an instance', () => {
    const directive = new ParentDirective();
    expect(directive).toBeTruthy();
  });
});
